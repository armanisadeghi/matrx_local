# Matrx Local — Environment Configuration
# Copy this file to .env and fill in values.

# Required: API key for engine access (any value works for local development)
API_KEY=local-dev

# Optional: PostgreSQL connection string for persistent scrape cache.
# Without this, the scraper engine works but uses in-memory caching only
# (cache is lost on restart).
#
# To use your Supabase PostgreSQL instance:
#   DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
#
# To use a local PostgreSQL:
#   DATABASE_URL=postgresql://user:pass@localhost:5432/matrx_local
DATABASE_URL=

# Optional: Brave Search API key. Enables the Search and Research tools.
# Get one at https://brave.com/search/api/
BRAVE_API_KEY=

# Optional: Proxy lists (comma-separated). The scraper engine rotates through
# these when direct requests are blocked.
DATACENTER_PROXIES=
RESIDENTIAL_PROXIES=

# Optional: Force a specific port. Default is 22140, auto-scans 22140-22159.
# MATRX_PORT=22140

# Debug mode (True/False)
DEBUG=True

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=DEBUG
