# You can safely ignore this directory

This directory is just code form another project that needs to be incorporated into this system at some point, unless we've already got a better version of it.

For now, we're just safely storing the code.